/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 Project GreenJade contributors
 *
 * Desktop glibc graph batch10306: continuum product deepen shell lamp
 * (wave 3).
 *
 * Surface (unique symbols):
 *   uint32_t gj_continuum_product_deepen_shell_10306(void);
 *     - Returns 1 (continuum product-deepen shell surface ready for
 *       wave 3). Soft compile-time product tag; not a live shell
 *       process or path probe.
 *   uint32_t __gj_continuum_product_deepen_shell_10306  (alias)
 *   __libcgj_batch10306_marker = "libcgj-batch10306"
 *
 * Exclusive continuum CREATE-ONLY (10301-10310: continuum product
 * deepen wave 3). Unique gj_continuum_product_deepen_shell_10306
 * surface only; no multi-def. Distinct from
 * gj_continuum_shell_ready_10105, gj_shell_green_10200 (batch10191),
 * and gj_continuum_product_deepen_path_10305. No parent wires.
 * No __int128.
 *
 * Clean-room freestanding pure C (integer only). Compiles with
 * -ffreestanding -msse2 -Wall -Wextra -Werror. No malloc, no errno, no
 * libc. No third-party source copied.
 */

#include <stddef.h>
#include <stdint.h>

const char __libcgj_batch10306_marker[] = "libcgj-batch10306";

/* Continuum product deepen shell ready lamp. */
#define B10306_CPD_SHELL  1u

/* ---- freestanding helpers ---------------------------------------------- */

static uint32_t
b10306_shell(void)
{
	return B10306_CPD_SHELL;
}

/* ---- public surface ---------------------------------------------------- */

/*
 * gj_continuum_product_deepen_shell_10306 - CPD shell ready lamp.
 *
 * Always returns 1. Soft pure-data product tag. Does not call libc.
 * No parent wires.
 */
uint32_t
gj_continuum_product_deepen_shell_10306(void)
{
	(void)NULL;
	return b10306_shell();
}

/* ---- underscored alias ------------------------------------------------- */

uint32_t __gj_continuum_product_deepen_shell_10306(void)
    __attribute__((alias("gj_continuum_product_deepen_shell_10306")));
