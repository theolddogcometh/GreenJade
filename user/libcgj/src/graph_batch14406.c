/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 Project GreenJade contributors
 *
 * Desktop glibc graph batch14406: continuum product deepen shell lamp
 * (wave 44).
 *
 * Surface (unique symbols):
 *   uint32_t gj_continuum_product_deepen_shell_14406(void);
 *     - Returns 1 (continuum product-deepen shell surface ready for
 *       wave 44). Soft compile-time product tag; not a live shell
 *       process or path probe.
 *   uint32_t __gj_continuum_product_deepen_shell_14406  (alias)
 *   __libcgj_batch14406_marker = "libcgj-batch14406"
 *
 * Exclusive continuum CREATE-ONLY (14401-14410: continuum product
 * deepen wave 44, post-14400). Unique gj_continuum_product_deepen_shell_14406
 * surface only; no multi-def. Distinct from prior continuum_product_deepen / continuum_smoke_soft /
 * continuum_dyn_soft symbols (1430x/1420x/1410x/…). No parent wires.
 * No __int128.
 *
 * Clean-room freestanding pure C (integer only). Compiles with
 * -ffreestanding -msse2 -Wall -Wextra -Werror. No malloc, no errno, no
 * libc. No third-party source copied.
 */

#include <stddef.h>
#include <stdint.h>

const char __libcgj_batch14406_marker[] = "libcgj-batch14406";

/* Continuum product deepen shell ready lamp. */
#define B14406_CPD_SHELL  1u

/* ---- freestanding helpers ---------------------------------------------- */

static uint32_t
b14406_shell(void)
{
	return B14406_CPD_SHELL;
}

/* ---- public surface ---------------------------------------------------- */

/*
 * gj_continuum_product_deepen_shell_14406 - CPD shell ready lamp
 *
 * Always returns 1u. Soft pure-data product tag. Does not call
 * libc. No parent wires.
 */
uint32_t
gj_continuum_product_deepen_shell_14406(void)
{
	(void)NULL;
	return b14406_shell();
}

/* ---- underscored alias ------------------------------------------------- */

uint32_t __gj_continuum_product_deepen_shell_14406(void)
    __attribute__((alias("gj_continuum_product_deepen_shell_14406")));
