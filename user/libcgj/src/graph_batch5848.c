/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 Project GreenJade contributors
 *
 * Desktop glibc graph batch5848: dual-assert pair green lamp.
 *
 * Surface (unique symbols):
 *   uint32_t gj_dual_assert_green_5848(void);
 *     - Returns 1 (both dual-assert halves — MIT and Apache-2.0 — are
 *       green for the clean-room license continuum). Soft compile-time
 *       dual-assert conjunction readiness lamp.
 *   uint32_t __gj_dual_assert_green_5848  (alias)
 *   __libcgj_batch5848_marker = "libcgj-batch5848"
 *
 * Exclusive continuum CREATE-ONLY (5841-5850: clean-room license
 * dual-assert continuum — cleanroom_present_5841,
 * mit_license_assert_5842, apache_license_assert_5843,
 * dual_license_assert_5844, cleanroom_source_assert_5845,
 * no_third_party_copy_5846, spdx_dual_ok_5847,
 * dual_assert_green_5848, cleanroom_license_ready_5849,
 * batch_id_5850). Unique gj_dual_assert_green_5848 surface only;
 * no multi-def. Distinct from gj_dual_license_assert_5844 and
 * gj_mit_license_assert_5842. No parent wires. No __int128.
 *
 * Clean-room freestanding pure C (integer only). Compiles with
 * -ffreestanding -msse2 -Wall -Wextra -Werror. No malloc, no errno, no
 * libc. No third-party source copied.
 */

#include <stddef.h>
#include <stdint.h>

const char __libcgj_batch5848_marker[] = "libcgj-batch5848";

/* Dual-assert pair (MIT + Apache-2.0) green for this continuum. */
#define B5848_DUAL_ASSERT_GREEN  1u

/* ---- freestanding helpers ---------------------------------------------- */

static uint32_t
b5848_dual_assert_green(void)
{
	return B5848_DUAL_ASSERT_GREEN;
}

/* ---- public surface ---------------------------------------------------- */

/*
 * gj_dual_assert_green_5848 - report dual-assert pair green lamp.
 *
 * Always returns 1 (both halves green). Soft pure-data readiness tag.
 * Does not call libc. No parent wires.
 */
uint32_t
gj_dual_assert_green_5848(void)
{
	(void)NULL;
	return b5848_dual_assert_green();
}

/* ---- underscored alias ------------------------------------------------- */

uint32_t __gj_dual_assert_green_5848(void)
    __attribute__((alias("gj_dual_assert_green_5848")));
