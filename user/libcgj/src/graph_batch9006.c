/*
 * SPDX-License-Identifier: MIT OR Apache-2.0
 * Copyright (c) 2026 Project GreenJade contributors
 *
 * Desktop glibc graph batch9006: continuum libcgj ready lamp.
 *
 * Surface (unique symbols):
 *   uint32_t gj_continuum_libcgj_ready_9006(void);
 *     - Returns 1 (continuum libcgj surface ready for the post-9000
 *       continuum product deepen wave). Soft compile-time product tag;
 *       not a live library load probe.
 *   uint32_t __gj_continuum_libcgj_ready_9006  (alias)
 *   __libcgj_batch9006_marker = "libcgj-batch9006"
 *
 * Exclusive continuum CREATE-ONLY (9001-9010: post-9000 continuum
 * product deepen). Unique gj_continuum_libcgj_ready_9006 surface only;
 * no multi-def. Distinct from gj_libcgj_green_9000 (batch8992),
 * gj_continuum_libcgj_ready_8206, gj_libcgj_ready_p (batch2595), and
 * gj_continuum_shell_ready_9005. No parent wires. No __int128.
 *
 * Clean-room freestanding pure C (integer only). Compiles with
 * -ffreestanding -msse2 -Wall -Wextra -Werror. No malloc, no errno, no
 * libc. No third-party source copied.
 */

#include <stddef.h>
#include <stdint.h>

const char __libcgj_batch9006_marker[] = "libcgj-batch9006";

/* Continuum libcgj ready lamp. */
#define B9006_LIBCGJ_READY  1u

/* ---- freestanding helpers ---------------------------------------------- */

static uint32_t
b9006_ready(void)
{
	return B9006_LIBCGJ_READY;
}

/* ---- public surface ---------------------------------------------------- */

/*
 * gj_continuum_libcgj_ready_9006 - continuum libcgj ready lamp.
 *
 * Always returns 1. Soft pure-data product tag. Does not call libc.
 * No parent wires.
 */
uint32_t
gj_continuum_libcgj_ready_9006(void)
{
	(void)NULL;
	return b9006_ready();
}

/* ---- underscored alias ------------------------------------------------- */

uint32_t __gj_continuum_libcgj_ready_9006(void)
    __attribute__((alias("gj_continuum_libcgj_ready_9006")));
